[package]
name = "cosmopad-token"
version = "0.1.0"
edition = "2021"
license = "MIT"
description = "CosmoPad standard CW20 token contract"

[lib]
crate-type = ["cdylib", "rlib"]

[dependencies]
cosmwasm-std = { version = "2.1.3", default-features = false, features = ["std"] }
cw20-base = "2.0.0"
